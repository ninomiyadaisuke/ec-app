import * as Actions from "./actions"
import initialState from "../store/initiaclState"

export const ProductsReducer = (state = initialState.products, action) => {
  switch (action.type) {

    default:
      return state
  }
}